<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ==================== TREE VIEW ==================== -->
    <record id="view_asset_maintenance_prediction_tree" model="ir.ui.view">
        <field name="name">asset.maintenance.prediction.tree</field>
        <field name="model">asset.maintenance.prediction</field>
        <field name="arch" type="xml">
            <tree decoration-danger="risk_level=='critical'" 
                  decoration-warning="risk_level=='high'"
                  decoration-info="risk_level=='medium'">
                <header>
                    <button name="batch_predict_all_assets" string="🚀 Tạo dự đoán hàng loạt" 
                            type="object" class="btn-primary"/>
                </header>
                <field name="asset_id"/>
                <field name="asset_category_id"/>
                <field name="next_maintenance_date"/>
                <field name="predicted_cost" widget="monetary"/>
                <field name="confidence_score" widget="progressbar"/>
                <field name="risk_level" widget="badge" 
                       decoration-danger="risk_level=='critical'"
                       decoration-warning="risk_level=='high'"/>
                <field name="maintenance_type"/>
                <field name="state" widget="badge"/>
            </tree>
        </field>
    </record>

    <!-- ==================== FORM VIEW ==================== -->
    <record id="view_asset_maintenance_prediction_form" model="ir.ui.view">
        <field name="name">asset.maintenance.prediction.form</field>
        <field name="model">asset.maintenance.prediction</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_schedule_maintenance" string="📅 Lên lịch bảo trì" 
                            type="object" class="oe_highlight"
                            attrs="{'invisible': [('state', '!=', 'predicted')]}"/>
                    <button name="action_retrain_model" string="🤖 Train AI Model" 
                            type="object" class="btn-primary"/>
                    <button name="action_view_cost_trends" string="📊 Xem xu hướng chi phí" 
                            type="object"/>
                    <field name="state" widget="statusbar"/>
                </header>
                
                <sheet>
                    <widget name="web_ribbon" title="Rủi ro cao" bg_color="bg-danger" 
                            attrs="{'invisible': [('risk_level', 'not in', ['high', 'critical'])]}"/>
                    
                    <group>
                        <group string="Thông tin tài sản">
                            <field name="asset_id"/>
                            <field name="asset_category_id"/>
                            <field name="prediction_date"/>
                        </group>
                        <group string="Dự đoán">
                            <field name="next_maintenance_date"/>
                            <field name="predicted_cost" widget="monetary"/>
                            <field name="confidence_score" widget="progressbar"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    
                    <group>
                        <group string="Chi tiết">
                            <field name="maintenance_type"/>
                            <field name="risk_level" widget="badge"/>
                            <field name="usage_hours" attrs="{'invisible': [('usage_hours', '=', 0)]}"/>
                        </group>
                        <group string="Lý do &amp; Ghi chú">
                            <field name="prediction_reason" nolabel="1"/>
                            <field name="notes" nolabel="1" placeholder="Ghi chú thêm..."/>
                        </group>
                    </group>
                </sheet>
                
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ==================== KANBAN VIEW ==================== -->
    <record id="view_asset_maintenance_prediction_kanban" model="ir.ui.view">
        <field name="name">asset.maintenance.prediction.kanban</field>
        <field name="model">asset.maintenance.prediction</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="asset_id"/>
                <field name="next_maintenance_date"/>
                <field name="predicted_cost"/>
                <field name="risk_level"/>
                <field name="state"/>
                
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="asset_id"/>
                                    </strong>
                                </div>
                                <span t-attf-class="badge badge-pill badge-#{record.risk_level.raw_value == 'critical' ? 'danger' : record.risk_level.raw_value == 'high' ? 'warning' : 'info'}">
                                    <field name="risk_level"/>
                                </span>
                            </div>
                            
                            <div class="o_kanban_record_body">
                                <div><i class="fa fa-calendar" title="Ngày bảo trì"/> <field name="next_maintenance_date"/></div>
                                <div><i class="fa fa-money" title="Chi phí"/> <field name="predicted_cost" widget="monetary"/></div>
                                <div class="text-muted">
                                    <field name="confidence_score"/>% độ tin cậy
                                </div>
                            </div>
                            
                            <div class="o_kanban_record_bottom">
                                <field name="state" widget="badge"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ==================== SEARCH VIEW ==================== -->
    <record id="view_asset_maintenance_prediction_search" model="ir.ui.view">
        <field name="name">asset.maintenance.prediction.search</field>
        <field name="model">asset.maintenance.prediction</field>
        <field name="arch" type="xml">
            <search>
                <field name="asset_id"/>
                <field name="asset_category_id"/>
                
                <filter string="Rủi ro cao" name="high_risk" 
                        domain="[('risk_level', 'in', ['high', 'critical'])]"/>
                <filter string="Dự đoán chưa xử lý" name="predicted" 
                        domain="[('state', '=', 'predicted')]"/>
                <filter string="Đã lên lịch" name="scheduled" 
                        domain="[('state', '=', 'scheduled')]"/>
                
                <separator/>
                <filter string="Tuần này" name="this_week" 
                        domain="[('next_maintenance_date', '>=', (context_today()-datetime.timedelta(days=7)).strftime('%Y-%m-%d')),
                                 ('next_maintenance_date', '&lt;=', (context_today()+datetime.timedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <filter string="Tháng này" name="this_month" 
                        domain="[('next_maintenance_date', '>=', time.strftime('%Y-%m-01')),
                                 ('next_maintenance_date', '&lt;', (datetime.date.today()+relativedelta(months=1)).strftime('%Y-%m-01'))]"/>
                
                <group expand="0" string="Nhóm theo">
                    <filter string="Danh mục" name="group_category" context="{'group_by': 'asset_category_id'}"/>
                    <filter string="Mức độ rủi ro" name="group_risk" context="{'group_by': 'risk_level'}"/>
                    <filter string="Trạng thái" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ==================== ACTION ==================== -->
    <record id="action_asset_maintenance_prediction" model="ir.actions.act_window">
        <field name="name">🤖 XGBoost AI - Dự đoán bảo trì</field>
        <field name="res_model">asset.maintenance.prediction</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'search_default_high_risk': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                🤖 XGBoost AI - Dự đoán bảo trì tài sản
            </p>
            <p>
                <strong>Powered by XGBoost với 1000 dữ liệu training</strong><br/><br/>
                Hệ thống AI sẽ phân tích và dự đoán:<br/>
                • 📅 Thời điểm cần bảo trì (độ chính xác ~85%)<br/>
                • 💰 Chi phí ước tính<br/>
                • ⚠️ Mức độ rủi ro (low/medium/high/critical)<br/>
                <br/>
                Click "Tạo dự đoán hàng loạt" để bắt đầu!
            </p>
        </field>
    </record>

    <!-- Action mở tài sản từ prediction -->
    <record id="action_asset_view_from_prediction" model="ir.actions.act_window">
        <field name="name">Tài sản</field>
        <field name="res_model">asset</field>
        <field name="view_mode">form</field>
        <field name="domain">[('id', '=', active_id)]</field>
    </record>

    <!-- ==================== WIZARD COST ANALYSIS ==================== -->
    <record id="view_asset_cost_analysis_wizard_form" model="ir.ui.view">
        <field name="name">asset.cost.analysis.wizard.form</field>
        <field name="model">asset.cost.analysis.wizard</field>
        <field name="arch" type="xml">
            <form string="Phân tích chi phí tài sản">
                <sheet>
                    <group>
                        <group string="Bộ lọc">
                            <field name="category_ids" widget="many2many_tags"/>
                            <field name="location_ids" widget="many2many_tags"/>
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </group>
                        <group string="Tùy chọn">
                            <field name="forecast_months"/>
                            <field name="include_replacement"/>
                        </group>
                    </group>
                    
                    <separator string="📊 Kết quả phân tích"/>
                    <group>
                        <group>
                            <field name="total_current_value" widget="monetary"/>
                            <field name="total_predicted_cost" widget="monetary"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                        <group>
                            <field name="high_risk_count"/>
                            <field name="avg_monthly_cost" widget="monetary"/>
                        </group>
                    </group>
                    
                    <notebook>
                        <page string="Chi tiết báo cáo">
                            <field name="analysis_result" widget="text" nolabel="1" readonly="1"/>
                        </page>
                    </notebook>
                </sheet>
                
                <footer>
                    <button name="action_generate_predictions" string="🤖 Tạo dự đoán AI" 
                            type="object" class="btn-primary"/>
                    <button name="action_schedule_all_maintenance" string="📅 Lên lịch tất cả" 
                            type="object"/>
                    <button name="action_export_report" string="📥 Xuất báo cáo" 
                            type="object"/>
                    <button string="Đóng" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_asset_cost_analysis_wizard" model="ir.actions.act_window">
        <field name="name">Phân tích chi phí AI</field>
        <field name="res_model">asset.cost.analysis.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- ==================== SERVER ACTIONS ==================== -->
    <record id="action_batch_predict_all" model="ir.actions.server">
        <field name="name">🤖 AI: Dự đoán tất cả tài sản</field>
        <field name="model_id" ref="model_asset_maintenance_prediction"/>
        <field name="binding_model_id" ref="model_asset_maintenance_prediction"/>
        <field name="state">code</field>
        <field name="code">
action = model.batch_predict_all_assets()
        </field>
    </record>

    <record id="action_retrain_xgboost" model="ir.actions.server">
        <field name="name">🔄 Train lại XGBoost Model</field>
        <field name="model_id" ref="model_asset_maintenance_prediction"/>
        <field name="binding_model_id" ref="model_asset_maintenance_prediction"/>
        <field name="state">code</field>
        <field name="code">
action = model.action_retrain_model()
        </field>
    </record>

    <!-- ==================== MENU ==================== -->
    <menuitem id="menu_asset_ai_root" name="🤖 XGBoost AI" 
              parent="nhan_su.menu_asset_root" sequence="50"/>
    
    <menuitem id="menu_asset_maintenance_prediction" name="Dự đoán bảo trì" 
              parent="menu_asset_ai_root" sequence="10"
              action="action_asset_maintenance_prediction"/>
</odoo>
